# Plugin dependencies (funpay-universal module)
# HTTP к hub и Roblox — стандартный urllib в потоках (aiohttp НЕ используется:
# привязка к event loop funpay-universal даёт «Event loop is closed»)
aiogram>=3.15.0
loguru>=0.7.3
